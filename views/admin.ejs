<!DOCTYPE html>
<html>
<head>
    <title>Admin - E-BOOK JESTRI</title>
    <style>
        body { font-family: sans-serif; padding: 20px; background: #f4f4f4; }
        .box { background: white; padding: 20px; border-radius: 8px; }
        table { width: 100%; border-collapse: collapse; margin-top: 20px; }
        th, td { border: 1px solid #ddd; padding: 10px; text-align: left; }
        .logout { color: red; float: right; }
    </style>
</head>
<body>
    <div class="box">
        <a href="/logout" class="logout">Logout</a>
        <h2>Dashboard Admin Jestri</h2>
        <hr>
        
        <form action="/tambah-buku" method="POST">
            <input type="text" name="judul" placeholder="Judul Buku" required>
            <input type="number" name="harga" placeholder="Harga" required>
            <button type="submit" style="background: green; color: white;">Tambah Buku</button>
        </form>

        <table>
            <tr style="background: #eee;">
                <th>Judul</th>
                <th>Harga</th>
                <th>Aksi</th>
            </tr>
            <% buku.forEach(item => { %>
            <tr>
                <td><%= item.judul %></td>
                <td><%= item.harga %></td>
                <td>
                    <a href="/hapus-buku/<%= item.id %>" style="color: red;" onclick="return confirm('Yakin hapus?')">Hapus</a>
                </td>
            </tr>
            <% }) %>
        </table>
    </div>
</body>
</html>

