<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Panel Admin | E-BOOK JESTRI</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        body { font-family: 'Segoe UI', Arial, sans-serif; background-color: #f4f7f6; margin: 0; padding: 20px; }
        .admin-container { max-width: 600px; margin: 0 auto; background: white; padding: 30px; border-radius: 15px; box-shadow: 0 10px 25px rgba(0,0,0,0.1); }
        h1, h2 { text-align: center; color: #333; }
        .card { background: #fff; border: 1px solid #eee; padding: 20px; border-radius: 10px; margin-bottom: 20px; }
        
        /* Form Styling */
        .form-group { margin-bottom: 15px; }
        label { display: block; margin-bottom: 5px; font-weight: bold; font-size: 14px; }
        input[type="text"], input[type="password"], input[type="file"], select, textarea {
            width: 100%; padding: 12px; border: 1px solid #ddd; border-radius: 8px; box-sizing: border-box; font-size: 14px;
        }
        textarea { height: 80px; resize: none; }
        
        /* Button Styling */
        .btn-add { background: #28a745; color: white; border: none; padding: 15px; width: 100%; border-radius: 8px; font-weight: bold; cursor: pointer; font-size: 16px; transition: 0.3s; }
        .btn-add:hover { background: #218838; }
        .btn-logout { display: block; text-align: center; margin-top: 20px; color: #dc3545; text-decoration: none; font-weight: bold; }
        
        /* Status Badge */
        .badge-security { background: #e8f5e9; color: #2e7d32; padding: 5px 10px; border-radius: 5px; font-size: 12px; display: inline-block; margin-bottom: 10px; }
    </style>
</head>
<body>

    <div class="admin-container">
        
        <% if (mode === 'login') { %>
            <h1>Login Admin</h1>
            <div class="badge-security"><i class="fa fa-shield-alt"></i> Sistem Terenkripsi</div>
            <form action="/login-admin" method="POST">
                <div class="form-group">
                    <label>Password Rahasia</label>
                    <input type="password" name="password" placeholder="Masukkan Password" required>
                </div>
                <button type="submit" class="btn-add" style="background: #007bff;">MASUK PANEL</button>
            </form>
            <a href="/" style="display:block; text-align:center; margin-top:15px; text-decoration:none; color:#666;">Kembali ke Beranda</a>
        <% } %>

        <% if (mode === 'dashboard') { %>
            <h1>Panel Kontrol</h1>
            <div class="badge-security"><i class="fa fa-check-circle"></i> Sesi Aktif: Admin Jestri</div>

            <div class="card">
                <h2><i class="fa fa-plus-circle"></i> Tambah E-Book</h2>
                <form action="/add-book" method="POST" enctype="multipart/form-data">
                    <div class="form-group">
                        <input type="text" name="title" placeholder="Judul E-Book" required>
                    </div>
                    <div class="form-group">
                        <select name="genre">
                            <option>Fiksi</option>
                            <option>Edukasi</option>
                            <option>Teknologi</option>
                            <option>Bisnis</option>
                        </select>
                    </div>
                    <div class="form-group">
                        <input type="text" name="price" placeholder="Harga (Contoh: 50.000)" required>
                    </div>
                    <div class="form-group">
                        <textarea name="description" placeholder="Nama Penulis / Sinopsis Singkat"></textarea>
                    </div>
                    <div class="form-group">
                        <label>Foto Sampul (JPG/PNG)</label>
                        <input type="file" name="image" accept="image/*" required>
                    </div>
                    <button type="submit" class="btn-add">PUBLIKASIKAN BUKU</button>
                </form>
            </div>

            <div class="card" style="border: 2px solid #007bff; background: #f0f7ff;">
                <h2 style="color: #007bff;"><i class="fa fa-file-shield"></i> Watermark Lab</h2>
                <p style="font-size: 12px; color: #444; margin-bottom: 15px;">
                    Tempelkan identitas <b>"E-BOOK JESTRI"</b> ke file PDF sebelum dikirim ke pembeli.
                </p>
                <form action="/secure-pdf" method="POST" enctype="multipart/form-data">
                    <div class="form-group">
                        <input type="file" name="pdfFile" accept="application/pdf" required>
                    </div>
                    <button type="submit" style="background: #007bff; color: white; border: none; padding: 12px; width: 100%; border-radius: 8px; font-weight: bold; cursor: pointer;">
                        PROSES & DOWNLOAD PDF
                    </button>
                </form>
            </div>

            <div style="text-align: center; margin-top: 10px;">
                <a href="/cek-keamanan-jestri" target="_blank" style="font-size: 12px; color: #999; text-decoration: none;">
                    <i class="fa fa-history"></i> Lihat Log Percobaan Peretasan
                </a>
            </div>

            <a href="/logout" class="btn-logout"><i class="fa fa-sign-out-alt"></i> Keluar Sistem</a>
        <% } %>

    </div>

</body>
</html>

