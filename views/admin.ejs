<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Jestri Admin | Manage</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-slate-900 text-white pb-20">
    <nav class="bg-slate-800 p-4 border-b border-slate-700 sticky top-0 z-50">
        <div class="container mx-auto flex justify-between items-center">
            <h1 class="text-xl font-bold text-blue-400">JESTRI CONTROL</h1>
            <a href="/logout" class="bg-red-500/20 text-red-400 border border-red-500/50 px-4 py-2 rounded-xl text-sm hover:bg-red-500 hover:text-white transition-all">Logout</a>
        </div>
    </nav>

    <main class="container mx-auto p-6 max-w-4xl">
        <section class="bg-slate-800 p-8 rounded-3xl border border-slate-700 shadow-2xl mb-10">
            <h2 class="text-2xl font-bold mb-6 flex items-center gap-2">
                <span class="bg-blue-600 w-2 h-8 rounded-full"></span> Tambah Koleksi Baru
            </h2>
            <form action="/tambah-buku" method="POST" class="grid grid-cols-1 md:grid-cols-2 gap-4">
                <input type="text" name="judul" placeholder="Judul Buku" class="bg-slate-900 border border-slate-700 p-4 rounded-2xl outline-none focus:ring-2 focus:ring-blue-500" required>
                <input type="text" name="penulis" placeholder="Nama Penulis" class="bg-slate-900 border border-slate-700 p-4 rounded-2xl outline-none focus:ring-2 focus:ring-blue-500" required>
                <input type="number" name="harga" placeholder="Harga Buku (Rp)" class="bg-slate-900 border border-slate-700 p-4 rounded-2xl outline-none focus:ring-2 focus:ring-blue-500" required>
                <input type="text" name="gambar" placeholder="Link URL Gambar Buku" class="bg-slate-900 border border-slate-700 p-4 rounded-2xl outline-none focus:ring-2 focus:ring-blue-500" required>
                <button type="submit" class="md:col-span-2 bg-blue-600 hover:bg-blue-700 py-4 rounded-2xl font-bold shadow-lg shadow-blue-900/20 transition-all uppercase tracking-widest">Simpan ke Database</button>
            </form>
        </section>

        <section>
            <h2 class="text-xl font-bold mb-6">Koleksi Saat Ini (<%= buku.length %>)</h2>
            <div class="grid grid-cols-1 gap-4">
                <% buku.forEach(item => { %>
                <div class="bg-slate-800 p-4 rounded-2xl border border-slate-700 flex items-center justify-between group hover:border-blue-500/50 transition-all">
                    <div class="flex items-center gap-4">
                        <img src="<%= item.gambar %>" class="w-16 h-20 object-cover rounded-lg bg-slate-900" onerror="this.src='https://via.placeholder.com/150'">
                        <div>
                            <h3 class="font-bold text-lg"><%= item.judul %></h3>
                            <p class="text-slate-400 text-sm"><%= item.penulis %> • <span class="text-green-400">Rp <%= item.harga %></span></p>
                        </div>
                    </div>
                    <a href="/hapus-buku/<%= item.id %>" class="bg-red-500/10 hover:bg-red-500 p-3 rounded-xl text-red-500 hover:text-white transition-all" onclick="return confirm('Hapus buku ini?')">
                        <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16" />
                        </svg>
                    </a>
                </div>
                <% }) %>
                <% if(buku.length == 0) { %>
                    <div class="text-center py-20 border-2 border-dashed border-slate-800 rounded-3xl text-slate-600 italic">Belum ada buku yang diupload.</div>
                <% } %>
            </div>
        </section>
    </main>
</body>
</html>

