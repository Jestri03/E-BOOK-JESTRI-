<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Login - E-BOOK JESTRI</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        body { font-family: 'Segoe UI', sans-serif; background: #f0f2f5; margin: 0; display: flex; align-items: center; justify-content: center; height: 100vh; }
        .login-box { background: white; padding: 40px; border-radius: 20px; box-shadow: 0 15px 35px rgba(0,0,0,0.1); width: 100%; max-width: 380px; text-align: center; }
        .login-box h1 { font-size: 24px; color: #1c1e21; margin-bottom: 10px; }
        .login-box p { color: #606770; margin-bottom: 30px; font-size: 14px; }
        .input-group { position: relative; margin-bottom: 20px; }
        .input-group i { position: absolute; left: 15px; top: 15px; color: #888; }
        .input-group input { width: 100%; padding: 14px 14px 14px 45px; border: 1px solid #dddfe2; border-radius: 8px; box-sizing: border-box; font-size: 16px; outline: none; }
        .btn-submit { width: 100%; padding: 14px; background: #007bff; color: white; border: none; border-radius: 8px; font-size: 17px; font-weight: bold; cursor: pointer; transition: 0.3s; }
        .btn-submit:hover { background: #0056b3; }
        
        /* Dashboard Styling */
        .admin-nav { background: #333; color: white; padding: 15px; display: flex; justify-content: space-between; align-items: center; }
    </style>
</head>
<body>

    <% if (mode === 'login') { %>
        <div class="login-box">
            <div style="background: #007bff; width: 70px; height: 70px; border-radius: 50%; display: flex; align-items: center; justify-content: center; margin: 0 auto 20px;">
                <i class="fa fa-lock" style="color: white; font-size: 30px;"></i>
            </div>
            <h1>Admin Panel</h1>
            <p>Silakan masuk untuk mengelola buku</p>
            <form action="/login-admin" method="POST">
                <div class="input-group">
                    <i class="fa fa-key"></i>
                    <input type="password" name="password" placeholder="Password Admin" required>
                </div>
                <button type="submit" class="btn-submit">LOGIN</button>
            </form>
        </div>
    <% } else { %>
        <div style="width: 100%; height: 100vh; background: white;">
            <div class="admin-nav">
                <span>DASHBOARD ADMIN</span>
                <a href="/logout" style="color: #ff4d4d; text-decoration: none; font-weight: bold;">Keluar</a>
            </div>
            <div style="padding: 20px;">
                <h3>Selamat Datang, Jestri!</h3>
                <p>Gunakan bagian ini untuk menambah buku baru nantinya.</p>
            </div>
        </div>
    <% } %>

</body>
</html>

