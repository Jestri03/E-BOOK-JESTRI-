<!DOCTYPE html>
<html>
<head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Panel</title>
    <style>
        body { font-family: sans-serif; background: #f4f4f4; display: flex; justify-content: center; align-items: center; min-height: 100vh; margin: 0; }
        .container { background: white; padding: 25px; border-radius: 15px; box-shadow: 0 4px 15px rgba(0,0,0,0.1); width: 90%; max-width: 400px; text-align: center; }
        h2 { margin-bottom: 20px; color: #333; }
        input, select, textarea { width: 100%; padding: 12px; margin: 10px 0; border: 1px solid #ddd; border-radius: 8px; box-sizing: border-box; }
        .btn-submit { background: #28a745; color: white; border: none; padding: 15px; width: 100%; border-radius: 8px; font-weight: bold; cursor: pointer; font-size: 16px; }
        .login-box i { font-size: 40px; color: #333; margin-bottom: 10px; }
    </style>
</head>
<body>
    <div class="container">
        <% if (mode === 'login') { %>
            <div class="login-box">
                <h2 style="font-size: 24px;">Admin Panel</h2>
                <p>E-BOOK JESTRI</p>
                <form action="/admin-dashboard" method="POST">
                    <input type="password" name="password" placeholder="Password Admin" required>
                    <button type="submit" style="background:#007bff; color:white; border:none; padding:12px; width:100%; border-radius:8px; margin-top:10px">MASUK</button>
                </form>
            </div>

        <% } else if (mode === 'menu-selection') { %>
            <h2>Pilih Mode</h2>
            <a href="/admin/katalog" style="display:block; background:#007bff; color:white; padding:15px; margin:10px; border-radius:8px; text-decoration:none">📂 KELOLA KATALOG</a>
            <a href="/admin/watermark-lab" style="display:block; background:#28a745; color:white; padding:15px; margin:10px; border-radius:8px; text-decoration:none">🧪 WATERMARK LAB</a>

        <% } else if (mode === 'katalog') { %>
            <h2><i class="fa fa-plus-circle"></i> Tambah E-Book</h2>
            <form action="#">
                <input type="text" placeholder="Judul E-Book">
                <select>
                    <option>Fiksi</option>
                    <option>Edukasi</option>
                    <option>Bisnis</option>
                </select>
                <input type="text" placeholder="Harga (Contoh: 50.000)">
                <textarea placeholder="Nama Penulis / Sinopsis Singkat"></textarea>
                <p style="text-align:left; margin:5px 0">Foto Sampul (JPG/PNG)</p>
                <input type="file">
                <button type="button" class="btn-submit">PUBLIKASIKAN BUKU</button>
            </form>
            <a href="/login-admin" style="display:block; margin-top:15px; color:#666; text-decoration:none">Kembali</a>
        <% } %>
    </div>
</body>
</html>

