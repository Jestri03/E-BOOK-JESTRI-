<!DOCTYPE html>
<html>
<head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ADMIN JESTRI</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        body { font-family: sans-serif; background: #f0f2f5; padding: 20px; }
        .card { background: white; padding: 20px; border-radius: 15px; box-shadow: 0 4px 10px rgba(0,0,0,0.1); max-width: 400px; margin: auto; text-align: center; }
        input { width: 100%; padding: 12px; margin: 8px 0; border: 1px solid #ddd; border-radius: 8px; box-sizing: border-box; }
        .btn { width: 100%; padding: 12px; background: #007bff; color: white; border: none; border-radius: 8px; font-weight: bold; cursor: pointer; }
        .book-item { display: flex; justify-content: space-between; padding: 10px; border-bottom: 1px solid #eee; }
    </style>
</head>
<body>
    <% if (mode === 'login') { %>
        <div class="card">
            <h2>Login Admin</h2>
            <form action="/admin-dashboard" method="POST">
                <input type="password" name="password" placeholder="Password" required>
                <button type="submit" class="btn">MASUK</button>
            </form>
        </div>
    <% } else { %>
        <div class="card">
            <h3>+ Tambah Koleksi</h3>
            <form action="/add-book" method="POST">
                <input type="text" name="title" placeholder="1. Nama Buku" required>
                <input type="text" name="author" placeholder="2. Penulis Buku" required>
                <input type="text" name="price" placeholder="3. Harga (Contoh: 2.700)" required>
                <input type="text" name="imageUrl" placeholder="4. Link URL Gambar Buku" required>
                <button type="submit" class="btn">PUBLIKASIKAN</button>
            </form>
            <hr>
            <h3>Hapus Koleksi</h3>
            <% books.forEach(book => { %>
                <div class="book-item">
                    <span><%= book.title %></span>
                    <a href="/delete-book/<%= book.id %>" style="color:red;"><i class="fa fa-trash"></i></a>
                </div>
            <% }) %>
            <a href="/" style="display:block; margin-top:10px; color:#666; text-decoration:none;">Kembali</a>
        </div>
    <% } %>
</body>
</html>

