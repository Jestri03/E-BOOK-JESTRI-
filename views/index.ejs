<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>E-BOOK JESTRI</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        body { font-family: 'Segoe UI', Arial, sans-serif; margin: 0; padding-bottom: 80px; background-color: #fcfcfc; }
        .navbar { display: flex; align-items: center; padding: 12px 15px; background: white; border-bottom: 1px solid #eee; position: sticky; top: 0; z-index: 1000; }
        .btn-menu { background: #007bff; color: white; border: none; padding: 8px 16px; border-radius: 4px; font-weight: bold; cursor: pointer; }
        .brand-title { flex-grow: 1; text-align: center; font-size: 20px; font-weight: bold; margin-right: 60px; }
        .sidebar { height: 100%; width: 0; position: fixed; z-index: 2000; top: 0; left: 0; background-color: white; overflow-x: hidden; transition: 0.2s; box-shadow: 2px 0 10px rgba(0,0,0,0.1); }
        .menu-item { padding: 12px 25px; text-decoration: none; font-size: 15px; color: #333; display: flex; align-items: center; gap: 10px; border-bottom: 1px solid #f5f5f5; cursor: pointer; }
        .donate-container { text-align: center; padding: 25px 0; }
        .btn-donate { background: #ff4d4d; color: white; padding: 12px 35px; border-radius: 30px; text-decoration: none; font-weight: bold; display: inline-flex; align-items: center; gap: 8px; }
        .search-box { display: flex; max-width: 400px; margin: 20px auto; border: 2px solid #444; border-radius: 10px; overflow: hidden; }
        .search-box input { flex: 1; padding: 14px; border: none; outline: none; }
        .search-box button { background: #444; color: white; border: none; padding: 0 18px; }

        /* Desain Daftar Buku */
        .book-grid { display: grid; grid-template-columns: repeat(2, 1fr); gap: 15px; padding: 15px; }
        .book-card { background: white; border-radius: 12px; box-shadow: 0 4px 10px rgba(0,0,0,0.05); overflow: hidden; border: 1px solid #eee; }
        .book-card img { width: 100%; height: 180px; object-fit: cover; }
        .book-info { padding: 10px; text-align: center; }
        .book-title { font-size: 14px; font-weight: bold; margin: 5px 0; color: #333; }
        .book-price { color: #007bff; font-weight: bold; font-size: 15px; }
    </style>
</head>
<body>

    <div id="mySidebar" class="sidebar">
        <span onclick="closeNav()" style="font-size: 30px; padding: 10px 20px; cursor: pointer; color: red; display: block;">&times;</span>
        <a href="/" class="menu-item"><i class="fa fa-home"></i> Rumah</a>
        <div style="padding: 15px 25px 5px; font-size: 12px; font-weight: bold; color: #007bff;">GENRE</div>
        <div onclick="closeNav()" class="menu-item">Fiksi</div>
        <div onclick="closeNav()" class="menu-item">Edukasi</div>
        <div onclick="closeNav()" class="menu-item">Teknologi</div>
    </div>

    <div class="navbar">
        <button class="btn-menu" onclick="openNav()">Menu</button>
        <div class="brand-title">E-BOOK JESTRI</div>
    </div>

    <div class="donate-container">
        <a href="https://link.dana.id/qr/8u8o6t3" target="_blank" class="btn-donate"><i class="fa fa-heart"></i> DONATE</a>
    </div>

    <div style="text-align: center; font-weight: bold;">Cari Buku / Penulis</div>
    <div class="search-box">
        <input type="text" placeholder="Ketik judul buku...">
        <button><i class="fa fa-search"></i></button>
    </div>

    <div class="book-grid">
        <% if (books && books.length > 0) { %>
            <% books.forEach(book => { %>
                <div class="book-card">
                    <img src="/uploads/<%= book.image %>" alt="Cover">
                    <div class="book-info">
                        <div class="book-title"><%= book.title %></div>
                        <div class="book-price">Rp <%= book.price %></div>
                        <div style="font-size: 11px; color: #888; margin-top: 5px;"><%= book.genre %></div>
                    </div>
                </div>
            <% }) %>
        <% } else { %>
            <div style="grid-column: span 2; text-align: center; padding: 40px; color: #999;">Belum ada buku tersedia.</div>
        <% } %>
    </div>

    <script>
        function openNav() { document.getElementById("mySidebar").style.width = "260px"; }
        function closeNav() { document.getElementById("mySidebar").style.width = "0"; }
    </script>
</body>
</html>

