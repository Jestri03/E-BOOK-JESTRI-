<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>E-Book JESTRI</title>
    <style>
        body { font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; background: #1a1a1a; color: white; margin: 0; padding: 20px; }
        .container { max-width: 1200px; margin: auto; }
        h1 { text-align: center; color: #f39c12; }
        .grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(250px, 1fr)); gap: 25px; padding: 20px 0; }
        .card { background: #2c3e50; border-radius: 15px; overflow: hidden; transition: 0.3s; box-shadow: 0 10px 20px rgba(0,0,0,0.5); }
        .card:hover { transform: translateY(-10px); }
        .card img { width: 100%; height: 350px; object-fit: cover; }
        .info { padding: 15px; text-align: center; }
        .price { font-size: 1.4em; color: #2ecc71; font-weight: bold; }
        .btn-wa { display: block; background: #25d366; color: white; text-decoration: none; padding: 12px; border-radius: 8px; margin-top: 10px; font-weight: bold; }
    </style>
</head>
<body>
    <div class="container">
        <h1>📚 Katalog E-Book JESTRI</h1>
        <div class="grid">
            <% if (buku && buku.length > 0) { %>
                <% buku.forEach(item => { %>
                    <div class="card">
                        <img src="<%= item.gambar %>" onerror="this.src='https://via.placeholder.com/350x500?text=No+Image'">
                        <div class="info">
                            <h3><%= item.judul %></h3>
                            <p>Penulis: <%= item.penulis %></p>
                            <p class="price">Rp <%= Number(item.harga).toLocaleString('id-ID') %></p>
                            <a href="https://wa.me/628XXXXXXXXX?text=Saya+ingin+beli+buku+<%= item.judul %>" class="btn-wa">Beli via WhatsApp</a>
                        </div>
                    </div>
                <% }) %>
            <% } else { %>
                <p style="grid-column: 1/-1; text-align: center;">Belum ada buku. Silakan Admin isi data.</p>
            <% } %>
        </div>
    </div>
</body>
</html>

